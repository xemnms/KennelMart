package com.kennel.mart.kennelmart.enums;

/**
 * Verification status for user identity verification.
 * 
 * Tracks the status of a user's NU Laguna identity verification.
 */
public enum VerificationStatus {
    PENDING,
    VERIFIED,
    REJECTED
}
